//Import a library to help to create a component
import { AppRegistry } from 'react-native';
import App from './src/App';

//Create a component

//Render it to the device
AppRegistry.registerComponent('myFirstWordGame', () => App);
